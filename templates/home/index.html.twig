{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    <div class="test">
        <h1 >bientot ici hotel </h1>
        <p>site en construction</p>
        <button class="btn btn-warning">un bouton</button>

        {% for chose in choses %}
            <p>{{ chose.name }}</p>
        {% endfor %}

    </div>


{% endblock %}
