<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">Starry Night Hotel</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">


                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="">{{ "navbar.admin.overview"|trans }} </a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_establishment') }}">{{ "navbar.admin.establishment"|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_room_admin') }}">{{ "navbar.admin.rooms"|trans }} </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_reservation_admin_index') }}">{{ "navbar.admin.reservations"|trans }} </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_equipment_index') }}">{{ "navbar.admin.equipment"|trans }} </a>
                    </li>

                    {% elseif app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_room') }}">{{ "navbar.rooms"|trans }}</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">{{ "navbar.logout"|trans }}</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_profile') }}">Profile</a>
                        </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_room') }}">{{ "navbar.rooms"|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}"> {{ "navbar.signin"|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">{{ "navbar.signup"|trans }}</a>
                    </li>
                {% endif %}

            </ul>


        </div>
    </div>
</nav>